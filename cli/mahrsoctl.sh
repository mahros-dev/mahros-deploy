# this is place holder
# mahrosctl <command> [options]
# commands:
#   init        Initialize the environment
#   deploy      Deploy the application
#   destroy     Destroy the application
#   status      Show the status of the application
# Usage:
#   mahrosctl init
#   mahrosctl deploy
#   mahrosctl destroy
#   mahrosctl status
# implement it here,
# the init must clone the stacks from github.com/mahros-dev/mahros-deploy/<main-branch>/stacks/*.yaml